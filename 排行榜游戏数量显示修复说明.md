# 排行榜游戏数量显示修复说明

## 问题描述
排行榜中显示的公司信息中，游戏数量显示为0，但实际上应该有游戏。

## 问题原因
在 `CompetitorContent` 中创建玩家公司数据时，`games` 字段被设置为空列表：
```kotlin
val playerCompanyData = CompetitorCompany(
    id = -1,
    name = saveData.companyName,
    logo = "🎮",
    marketValue = playerMarketValue,
    fans = saveData.fans,
    games = emptyList(), // 这里设置为空列表
    yearsFounded = saveData.currentYear,
    reputation = 0f
)
```

因此，在 `CompetitorCard` 中显示 `${competitor.games.size}款游戏` 时，玩家公司总是显示为0款游戏。

## 修复方案

### 1. 修改 `CompetitorCard` 组件
为 `CompetitorCard` 添加 `playerGameCount` 参数，用于传递玩家公司的游戏数量。

**文件**：`app/src/main/java/com/example/yjcy/ui/CompetitorScreen.kt`

#### 修改内容

1. **添加参数**：
```kotlin
@Composable
fun CompetitorCard(
    rank: Int,
    competitor: CompetitorCompany,
    onClick: () -> Unit,
    isPlayer: Boolean = false,
    playerGameCount: Int = 0 // 玩家公司的游戏数量（仅当isPlayer为true时使用）
)
```

2. **修改显示逻辑**：
```kotlin
Text(
    text = "成立${competitor.yearsFounded}年 | ${if (isPlayer) playerGameCount else competitor.games.size}款游戏",
    color = Color.White.copy(alpha = 0.7f),
    fontSize = 12.sp
)
```

### 2. 修改调用处
在 `CompetitorContent` 中调用 `CompetitorCard` 时，传入玩家公司的真实游戏数量。

**修改内容**：
```kotlin
CompetitorCard(
    rank = index + 1,
    competitor = company,
    onClick = { 
        if (isPlayer) {
            showPlayerDetail = true
        } else {
            selectedCompetitor = company
        }
    },
    isPlayer = isPlayer,
    playerGameCount = if (isPlayer) saveData.games.size else 0
)
```

## 修复效果

### 修复前
- 玩家公司：显示 "成立X年 | 0款游戏"（无论实际有多少游戏）
- 竞争对手：显示 "成立X年 | X款游戏"（正确显示）

### 修复后
- 玩家公司：显示 "成立X年 | X款游戏"（正确显示实际游戏数量）
- 竞争对手：显示 "成立X年 | X款游戏"（保持正确显示）

## 技术细节

### 显示逻辑
- 当 `isPlayer = true` 时，使用 `playerGameCount`（即 `saveData.games.size`）
- 当 `isPlayer = false` 时，使用 `competitor.games.size`

### 数据来源
- **玩家公司游戏数量**：从 `saveData.games` 获取
- **竞争对手游戏数量**：从 `competitor.games` 获取

## 注意事项

1. **兼容性**：`playerGameCount` 参数有默认值 `0`，不会影响其他地方的调用
2. **数据准确性**：玩家公司的游戏数量直接从 `saveData.games.size` 获取，确保显示准确
3. **竞争对手显示**：竞争对手的游戏数量显示逻辑保持不变，继续使用 `competitor.games.size`

## 修改文件

- `app/src/main/java/com/example/yjcy/ui/CompetitorScreen.kt`
  - 第1247行：添加 `playerGameCount` 参数
  - 第1334行：修改显示逻辑
  - 第1205行：传入玩家游戏数量

## 总结

本次修复解决了排行榜中玩家公司游戏数量显示为0的问题，确保：
- ✅ 玩家公司的游戏数量正确显示
- ✅ 竞争对手的游戏数量显示逻辑保持不变
- ✅ 代码兼容性良好，不影响其他功能

现在排行榜可以正确显示所有公司的游戏数量了！








